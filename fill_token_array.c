#include "monty.h"
/**
 * fill_token_array - function to fill the token
 * @buffer: buffer storing the file content
 * @token: location to be filled
*/
void fill_token_array(char *buffer, char *token)
{
	strcpy(buffer, token);
}
